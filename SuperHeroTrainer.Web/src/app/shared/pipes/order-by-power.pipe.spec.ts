import { OrderByPowerPipe } from './order-by-power.pipe';

describe('OrderByPowerPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByPowerPipe();
    expect(pipe).toBeTruthy();
  });
});
